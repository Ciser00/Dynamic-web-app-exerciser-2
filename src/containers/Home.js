import React from `react`;

function home(){
  return (
    <main className="App">
      <header> Weather App </header>
      <section> Weather data </section>
      </main>
  );
}

export default Home;
